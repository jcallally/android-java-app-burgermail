<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BurgerMail" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Fuente del sistema -->
        <item name="android:fontFamily">@font/pangolin_regular</item>

    </style>

    <style name="Theme.BurgerMail" parent="Base.Theme.BurgerMail" />

    <!-- Tema para el splash-->
    <style name="Theme.BurgerMail.Splash" parent="Base.Theme.BurgerMail">
        <!-- Colores de barras del sistema -->
        <item name="android:statusBarColor">@color/colorWhite</item>
        <item name="android:navigationBarColor">@color/colorWhite</item>
    </style>

</resources>